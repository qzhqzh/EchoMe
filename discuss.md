# EchoMe 项目讨论背景

## 1. 项目名称

**EchoMe**

核心意象：让不同 AI 都能回应“真正的我”。EchoMe 强调的不是简单复制用户数据，而是让用户的习惯、背景、表达方式与思考脉络，在不同 AI 产品中持续回响。

## 2. 一句话定位

**EchoMe 是一个跨 AI 的个人上下文同步层，让用户的习惯、偏好、项目背景与协作方式，在 ChatGPT、Claude、Gemini 等不同 AI 产品之间自然延续。**

更口语化：

> 换一个 AI，不必重新介绍自己。

英文表达：

> Switch AI, not yourself.

或：

> Your context, echoed across every AI.

## 3. 项目背景

在 AI 时代，用户长期使用 ChatGPT、Claude、Gemini、Kimi、DeepSeek、本地 Agent、AI IDE 等产品后，会逐渐沉淀出大量隐性资产。

这些资产不只是聊天记录，而包括：

- 用户是谁；
- 用户正在做什么项目；
- 用户习惯怎样和 AI 沟通；
- 用户喜欢什么回答风格；
- 用户常用什么技术栈；
- 用户做决策时重视哪些因素；
- 用户有哪些长期项目背景；
- 用户不希望 AI 做什么；
- 用户和 AI 已经形成了怎样的协作默契。

但是这些信息通常被锁定在某一个 AI 平台中。当用户切换 AI 产品时，往往需要重新解释背景、重新调整风格、重新训练协作习惯。

用户真正需要的不是一次性的“导入 / 导出”，而是：

> 不管今天打开哪个 AI，它都能自然理解我是谁、我在做什么、我喜欢怎样协作。

EchoMe 试图解决的正是这个问题。

## 4. 核心问题

当前不同 AI 产品之间存在明显割裂。

### 4.1 上下文割裂

用户在 ChatGPT 中讨论过的项目，切换到 Claude 后无法自然接续。

### 4.2 习惯割裂

一个 AI 知道用户喜欢简洁回答，另一个 AI 仍然需要重新调教。

### 4.3 项目割裂

用户长期推进的项目背景散落在多个 AI 平台和多轮对话中，缺少统一沉淀。

### 4.4 记忆不可控

平台内置记忆通常黑箱化，用户难以清晰查看、修改、迁移、版本化管理。

### 4.5 导入导出体验笨重

手动导出聊天记录、上传文件、粘贴记忆摘要，对普通用户来说门槛高、频率低，不适合作为日常体验。

因此，EchoMe 不应定位为简单的“AI 迁移工具”，而应定位为：

> 一个独立于具体 AI 平台的个人上下文层。

## 5. 产品本质

EchoMe 的核心不是保存聊天记录，而是构建和维护一个用户自己的 **Personal Context Layer**。

它应该包含：

- 个人画像；
- 沟通偏好；
- 工作方式；
- 项目上下文；
- 长期知识；
- 最近讨论进度；
- AI 协作习惯；
- 安全边界与禁忌。

当用户在不同 AI 产品之间切换时，EchoMe 根据当前场景自动或半自动地把合适的上下文带过去。

最终用户体验应该是：

> 用户只是切换了模型或平台，但没有丢失自己的 AI 协作现场。

## 6. 产品目标

### 6.1 短期目标

先在个人环境中跑通完整闭环：

1. 从用户和 AI 的对话中提炼上下文；
2. 生成结构化的个人 / 项目上下文；
3. 用户可以查看、编辑、删除这些上下文；
4. 用户切换到另一个 AI 产品时，可以一键或自动挂载相关上下文；
5. 验证另一个 AI 是否能自然接上之前的讨论。

### 6.2 中期目标

形成一个跨 AI 的个人上下文仓库：

- 支持多 AI 产品；
- 支持项目级上下文；
- 支持不同使用场景的 Profile；
- 支持浏览器插件注入；
- 支持本地或云端同步；
- 支持 MCP / API 接入给开发者工具或 Agent 使用。

### 6.3 长期目标

成为个人或企业的 AI 上下文基础设施：

- 个人版：让用户在不同 AI 之间保留自己的习惯和项目连续性；
- 团队版：让组织沉淀团队 AI 使用规范、项目上下文、岗位知识和协作习惯；
- 企业版：支持 AI 平台切换、员工交接、部门知识治理、敏感信息管控。

## 7. 核心用户场景

### 场景 1：跨 AI 继续讨论同一个项目

用户昨天在 ChatGPT 中讨论 EchoMe 的产品定位。今天打开 Claude，直接输入：

> 继续昨天那个产品，我觉得导入导出还是太麻烦。

EchoMe 应该能识别：

- 用户指的是 EchoMe 项目；
- 之前已经讨论过“跨 AI 个人上下文同步”；
- 用户当前关注点是“无感同步，而不是手动导入导出”；
- 回答应围绕产品架构、交互体验和 MVP 展开。

然后自动或半自动注入相关背景，让 Claude 能直接接续。

### 场景 2：不同 AI 使用同一套个人偏好

用户长期偏好：

- 中文回答；
- 先给结论，再展开分析；
- 技术问题要给可执行方案；
- 架构问题要考虑长期扩展；
- 不喜欢空泛鼓励；
- 不希望 AI 擅自假设关键信息。

EchoMe 应该让这些偏好在不同 AI 中持续生效。

### 场景 3：项目上下文沉淀

用户长期推进多个项目，例如：

- EchoMe：跨 AI 个人上下文同步层；
- 数字资产管理系统；
- 生信流程标准化工具；
- 变异过滤规则引擎；
- AI 工单助手。

EchoMe 应该把这些项目分别沉淀为 Project Context，而不是混在一个杂乱记忆池中。

### 场景 4：开发者工具接入

用户在 Claude Code、Codex CLI、Cursor、Kiro 或本地 Agent 中开发项目时，EchoMe 可以通过 MCP / API 提供上下文。

例如：

- 当前项目的背景；
- 用户喜欢的技术栈；
- 用户常用目录结构；
- 用户希望代码助手遵循的架构原则；
- 项目已讨论过的设计决策。

### 场景 5：企业团队迁移

公司从一个 AI 平台迁移到另一个 AI 平台时，不只是迁移聊天记录，而是迁移：

- 团队 Prompt 规范；
- 部门知识背景；
- 项目上下文；
- 岗位协作规则；
- 员工偏好；
- 安全与合规边界。

这个是长期企业版方向，MVP 阶段暂不优先实现。

## 8. EchoMe 不是什么

为了避免项目失焦，需要明确 EchoMe 的边界。

EchoMe 不是：

1. **不是单纯聊天记录备份工具**：聊天记录只是原始材料，核心是提炼可用上下文。
2. **不是简单导入导出工具**：导入导出只能作为辅助能力，不能作为主体验。
3. **不是另一个普通 AI 聊天壳**：统一 AI 入口可以是未来方向，但 MVP 不一定要重做完整聊天产品。
4. **不是简单 Prompt 模板管理器**：Prompt 是表达形式，Context 才是核心资产。
5. **不是无限制自动记忆系统**：必须区分长期偏好、短期状态、项目上下文和敏感信息。
6. **不是黑箱用户画像系统**：用户必须能看到、修改、删除、确认自己的上下文。

## 9. 核心概念模型

EchoMe 的核心数据不应以“聊天记录”为中心，而应以 **Context Object** 为中心。

### 9.1 Persona Profile

用户长期画像。

包括：

- 语言偏好；
- 回答风格偏好；
- 沟通节奏；
- 决策方式；
- 常用技术栈；
- 常见任务类型；
- 禁忌和边界。

示例：

```json
{
  "type": "persona_profile",
  "language": "zh-CN",
  "response_style": ["direct", "structured", "analysis_first"],
  "technical_preferences": ["Docker", "Django", "Vue", "Linux", "Traefik"],
  "decision_preferences": ["long_term_extensibility", "practical_MVP", "risk_control"],
  "boundaries": ["do_not_assume_critical_info", "avoid_empty_encouragement"]
}
```

### 9.2 Project Context

某个长期项目的上下文。

包括：

- 项目名称；
- 项目目标；
- 当前阶段；
- 已确认的设计判断；
- 已否定的方案；
- 当前待讨论问题；
- 相关技术栈；
- 关键文档与对话摘要。

示例：

```json
{
  "type": "project_context",
  "name": "EchoMe",
  "goal": "构建跨 AI 的个人上下文同步层",
  "current_stage": "产品定义与 MVP 架构设计",
  "confirmed_decisions": [
    "不以手动导入导出作为主体验",
    "优先考虑浏览器插件 + Context Vault 的 MVP",
    "长期方向是 Personal Context Layer"
  ],
  "open_questions": [
    "如何做自动注入策略",
    "如何区分长期偏好和短期上下文",
    "如何设计用户可审查的记忆界面"
  ]
}
```

### 9.3 Recent Thread

最近讨论状态。它是短期上下文，不应直接污染长期画像。

包括：

- 最近一次讨论主题；
- 对话摘要；
- 未完成问题；
- 需要下次继续的方向；
- 所属项目。

### 9.4 Preference Rule

用户偏好的结构化规则。

示例：

```json
{
  "type": "preference_rule",
  "scope": "technical_answer",
  "rule": "回答技术问题时，优先给出可执行步骤和命令，再解释原理",
  "confidence": 0.86,
  "source": "conversation_summary",
  "user_confirmed": false
}
```

### 9.5 Boundary Rule

安全边界和禁止事项。

示例：

```json
{
  "type": "boundary_rule",
  "rule": "未经用户明确确认，不执行删除线上数据、发送邮件、公开发布等不可逆操作",
  "severity": "high",
  "user_confirmed": true
}
```

## 10. 上下文分层

EchoMe 需要明确区分不同类型的上下文。

### 10.1 长期上下文

稳定存在，适合跨平台长期使用。

例如：

- 用户偏好中文；
- 用户常用 Django、Vue、Docker；
- 用户喜欢架构化分析；
- 用户不喜欢空泛回答。

### 10.2 项目上下文

绑定具体项目，不应全局注入。

例如：

- EchoMe 当前讨论到 MVP 阶段；
- 生信变异过滤引擎使用 Django + PostgreSQL；
- 数字资产管理系统包含 Gateway、App、Engine、Infra 等层。

### 10.3 短期上下文

最近几轮对话状态，只在短时间内有效。

例如：

- 当前正在比较浏览器插件和统一 AI 工作台；
- 当前准备写开发上下文文档。

### 10.4 敏感上下文

需要特殊处理。

例如：

- 真实姓名；
- 账号信息；
- 公司内部资料；
- 私有代码；
- 客户数据；
- 服务器地址；
- 密钥；
- Token。

敏感上下文默认不自动注入，必须有明确授权和可见提示。

## 11. MVP 方案建议

### 11.1 MVP 名称

**EchoMe Companion**

### 11.2 MVP 形态

建议第一版采用：

```text
浏览器插件 + 本地/后端 Context Vault + 简单管理界面
```

不要一开始做完整 AI 聊天平台。

### 11.3 MVP 支持范围

第一版建议优先支持：

- ChatGPT 网页；
- Claude 网页；
- Gemini 网页。

后续再扩展：

- Perplexity；
- Kimi；
- DeepSeek；
- Grok；
- AI IDE；
- Claude Code；
- Codex CLI；
- Cursor；
- Kiro。

## 12. MVP 核心功能

### 12.1 对话捕获

浏览器插件在用户授权后，捕获用户与 AI 的对话内容。

要求：

- 用户明确授权；
- 可开启 / 关闭；
- 可按站点控制；
- 可按会话控制；
- 不默认上传敏感内容；
- 提供本地优先模式。

### 12.2 对话摘要

将原始聊天整理成摘要。摘要重点不是复述对话，而是提炼：

- 用户意图；
- 项目背景；
- 已做决策；
- 用户偏好；
- 未完成问题；
- 可复用上下文。

### 12.3 上下文抽取

从摘要中提取 Context Objects，包括：

- Persona Profile；
- Project Context；
- Recent Thread；
- Preference Rule；
- Boundary Rule。

### 12.4 上下文管理界面

用户可以查看和管理 EchoMe 学到的内容。

界面至少包括：

- 我的偏好；
- 我的项目；
- 最近讨论；
- 待确认记忆；
- 敏感信息；
- 已忽略内容。

每条上下文应支持：

- 查看来源；
- 编辑；
- 删除；
- 合并；
- 标记为长期有效；
- 标记为仅当前项目有效；
- 标记为不再使用。

### 12.5 上下文检索

当用户在某个 AI 页面输入问题时，EchoMe 判断是否需要挂载上下文。

检索维度：

- 当前网页平台；
- 当前会话标题；
- 用户输入内容；
- 最近活跃项目；
- 历史项目匹配；
- 关键词和语义相似度；
- 用户长期偏好。

### 12.6 上下文注入

EchoMe 将相关上下文注入到当前 AI 会话中。

早期建议采用半自动注入，而不是完全静默注入。

推荐交互：

```text
检测到你可能正在继续讨论：EchoMe 项目

可注入上下文：
- 项目定位
- 已确认设计决策
- 当前讨论焦点

[注入本次] [始终自动注入该项目] [忽略]
```

### 12.7 注入预览

用户可以看到 EchoMe 准备注入什么，避免用户觉得系统偷偷改写自己的问题。

注入内容应尽量简洁：

```text
你正在帮助用户继续讨论 EchoMe 项目。
项目目标：构建跨 AI 的个人上下文同步层。
已确认：不以手动导入导出为主体验，优先浏览器插件 + Context Vault。
用户偏好：中文、结构化、先判断再展开。
```

### 12.8 效果验证

MVP 可以提供简单验证能力：

- 同一问题在不同 AI 中表现是否更一致；
- 注入前后回答是否更贴合用户；
- 用户是否认为“这个 AI 更懂我了”；
- 哪些上下文被频繁使用；
- 哪些上下文从未使用，可以降权。

## 13. 推荐技术架构

### 13.1 总体架构

```text
Browser Extension
        ↓
Context Capture Layer
        ↓
Context Processing Pipeline
        ↓
Context Vault
        ↓
Retrieval & Injection Engine
        ↓
Target AI Products
```

### 13.2 Browser Extension

负责：

- 识别 AI 网站；
- 捕获用户输入与 AI 输出；
- 在页面上显示 EchoMe 控件；
- 注入上下文到输入框或会话中；
- 提供本地设置。

### 13.3 Context Capture Layer

负责：

- 会话采集；
- 文本清洗；
- 平台适配；
- 去重；
- 会话分段；
- 敏感信息初步识别。

### 13.4 Context Processing Pipeline

负责：

- 对话摘要；
- 偏好提取；
- 项目识别；
- 决策提取；
- 长短期分类；
- 可信度评分。

### 13.5 Context Vault

负责存储结构化上下文。

可以包括：

- SQLite / IndexedDB 本地存储；
- PostgreSQL 云端存储；
- 向量数据库；
- JSON / Markdown 导出。

### 13.6 Retrieval Engine

负责按需召回相关上下文。

检索方式：

- 关键词检索；
- 向量检索；
- 项目匹配；
- 时间衰减；
- 用户确认状态；
- 上下文类型过滤。

### 13.7 Injection Engine

负责将上下文转成目标 AI 能理解的 prompt。

不同平台可能需要不同注入策略：

- 会话开头注入；
- 输入框前置注入；
- 用户点击按钮后插入；
- 系统生成一段可复制背景；
- API 模式下作为 system / developer message 注入。

## 14. 技术路线选择

| 路线 | 用户是否换入口 | 无感程度 | 技术可控性 | 早期是否适合 |
|---|---:|---:|---:|---|
| 浏览器插件 | 不需要 | 较高 | 中 | 最适合第一阶段 |
| 统一 AI 工作台 | 需要 | 最高 | 最高 | 中后期 |
| MCP / API Context Server | 视场景 | 高 | 高 | 可作为底层能力同步设计 |

### 14.1 路线 A：浏览器插件

优点：

- 用户不需要改变原有 AI 使用习惯；
- 可覆盖 ChatGPT、Claude、Gemini 等网页产品；
- 适合作为个人 MVP；
- 能验证真实使用场景。

缺点：

- 各家网页 DOM 结构变化会带来维护成本；
- 移动端 App 覆盖弱；
- 平台可能限制注入行为；
- 自动注入需要谨慎设计。

结论：

> 最适合第一阶段。

### 14.2 路线 B：统一 AI 工作台

优点：

- 上下文控制最完整；
- 可以统一接入多模型 API；
- 更容易做稳定体验；
- 适合后期商业化。

缺点：

- 用户需要改变入口；
- 需要承担模型 API 成本；
- 难以复刻各家原生产品能力；
- 冷启动较重。

结论：

> 适合中后期，不建议 MVP 一开始就做。

### 14.3 路线 C：MCP / API Context Server

优点：

- 适合开发者和 Agent；
- 架构清晰；
- 方便接入 Claude Code、Codex、Cursor、Kiro、本地 Agent；
- 适合企业部署。

缺点：

- 对普通用户不够直观；
- 需要目标工具支持 MCP / API；
- 不直接覆盖普通网页 AI 产品。

结论：

> 可以作为底层能力同步设计，但 MVP 触达层优先插件。

## 15. 推荐开发阶段

### Phase 0：概念验证

目标：证明“上下文带过去后，新 AI 能明显更懂用户”。

实现方式：

- 手动整理一个 EchoMe 项目上下文；
- 手动注入到 ChatGPT / Claude / Gemini；
- 对比注入前后回答质量；
- 总结什么上下文最有用。

### Phase 1：本地 Context Vault

目标：先把上下文结构化。

功能：

- 新建项目上下文；
- 编辑个人偏好；
- 保存最近讨论摘要；
- 导出 Markdown / JSON；
- 手动复制到其他 AI。

这一步不一定需要插件，先跑通数据模型。

### Phase 2：浏览器插件半自动注入

目标：减少复制粘贴。

功能：

- 识别 ChatGPT / Claude / Gemini；
- 在输入框旁显示 EchoMe 按钮；
- 根据当前输入推荐上下文；
- 用户点击后注入；
- 支持注入预览。

### Phase 3：自动摘要与上下文抽取

目标：减少用户手动维护。

功能：

- 自动总结对话；
- 自动识别项目；
- 自动提取偏好；
- 生成待确认记忆；
- 用户周期性确认。

### Phase 4：智能检索与注入策略

目标：让体验更接近无感。

功能：

- 输入时自动判断是否需要上下文；
- 自动推荐最相关项目；
- 支持用户设置自动注入规则；
- 降低无关上下文误注入。

### Phase 5：MCP / API 接入

目标：进入开发者和企业场景。

功能：

- 提供 Context API；
- 提供 MCP Server；
- 让 Claude Code、Codex、Cursor、Kiro、本地 Agent 查询用户上下文；
- 支持上下文读写权限控制。

## 16. 最小可用版本功能清单

### MVP 必须有

- 用户 Profile 编辑；
- 项目 Context 编辑；
- 最近讨论摘要；
- 上下文检索；
- 一键生成注入 Prompt；
- 浏览器插件按钮；
- 注入预览；
- 用户确认 / 删除上下文。

### MVP 暂不必有

- 完整多用户系统；
- 企业权限体系；
- 全自动静默注入；
- 移动端支持；
- 完整 AI 聊天工作台；
- 多模型 API 聚合计费；
- 复杂团队协作。

## 17. 核心交互设计原则

### 17.1 不要打扰用户

EchoMe 应尽量低干扰，不应该每次用户打开 AI 页面都弹窗。

### 17.2 不要黑箱改写用户输入

如果系统注入上下文，用户最好能看见或至少能审计。

### 17.3 不要过度记忆

不是所有内容都值得成为长期记忆。

应区分：

- 临时对话；
- 项目进度；
- 长期偏好；
- 明确边界；
- 敏感信息。

### 17.4 先半自动，再逐步无感

早期不建议完全静默注入，应从可预览、可确认的半自动模式开始。

### 17.5 用户拥有最终控制权

上下文必须可查看、可编辑、可删除、可导出。

## 18. 隐私与安全边界

EchoMe 涉及用户长期上下文，因此隐私和安全是产品核心，而不是附加功能。

建议原则：

- 默认本地优先，尤其是早期个人版；
- 敏感信息识别后默认不自动注入；
- 用户可以设置某些站点不捕获；
- 用户可以设置某些项目不上传云端；
- 所有自动学习内容进入“待确认”区域，而不是直接成为长期记忆；
- 上下文注入前应支持预览；
- 企业版必须支持权限、审计、数据隔离和敏感信息治理。

## 19. 与现有开源项目的差异化

现有开源项目中已经有一些方向相近的实现，例如 Personal AI Memory、Aether、Vektori Memory、Mem0 Chrome Extension、OneBrain、Mem0、Basic Memory、Personal Context Portfolio 等。

这些项目证明跨 AI 记忆和上下文共享是真实需求。

但 EchoMe 的差异化不应只是“跨平台保存聊天记录”，而应聚焦在：

- 从聊天记录中提炼长期个人协作模型；
- 区分 Persona / Project / Recent Thread / Preference / Boundary；
- 让上下文在不同 AI 间按需生效；
- 强调用户可见、可控、可审查；
- 优先解决“换 AI 不必重新介绍自己”的连续性体验；
- 未来扩展到开发者 Agent 和企业组织上下文基础设施。

## 20. 面向代码助手的开发提示词

如果需要让 Claude Code、Codex、Cursor 等代码助手开发 EchoMe，可以使用下面这段背景提示词：

```text
我们正在开发 EchoMe，一个跨 AI 的个人上下文同步层。

项目目标：
让用户在 ChatGPT、Claude、Gemini 等不同 AI 产品之间切换时，仍能保留个人习惯、沟通偏好、项目背景和协作方式。EchoMe 不是简单聊天记录备份工具，也不是一次性导入导出工具，而是 Personal Context Layer。

MVP 形态：
浏览器插件 + Context Vault + 简单管理界面。

第一阶段优先功能：
1. 用户 Profile 编辑；
2. Project Context 编辑；
3. 最近讨论摘要；
4. 一键生成可注入 Prompt；
5. 浏览器插件在 ChatGPT / Claude / Gemini 页面提供注入按钮；
6. 用户可以预览、确认、删除上下文；
7. 暂不做完整 AI 聊天工作台和企业权限体系。

核心数据对象：
- Persona Profile
- Project Context
- Recent Thread
- Preference Rule
- Boundary Rule

关键设计原则：
- 本地优先；
- 用户可见可控；
- 不黑箱改写用户输入；
- 不默认注入敏感信息；
- 先半自动注入，再逐步自动化；
- 区分长期偏好、项目上下文和短期对话状态。

请基于这个背景设计项目目录结构、数据模型、接口、浏览器插件架构和最小可运行版本开发计划。
```

## 21. 建议仓库结构草案

```text
echome/
├── apps/
│   ├── web/                    # 管理界面
│   ├── extension/              # 浏览器插件
│   └── server/                 # 可选后端服务
│
├── packages/
│   ├── context-core/           # 上下文对象、规则、抽取逻辑
│   ├── context-vault/          # 本地/远程存储适配
│   ├── retrieval/              # 检索与召回
│   ├── injection/              # 注入 Prompt 生成
│   └── shared/                 # 共享类型、工具函数
│
├── docs/
│   ├── product.md
│   ├── architecture.md
│   ├── data-model.md
│   └── roadmap.md
│
├── examples/
│   ├── sample-profile.json
│   ├── sample-project-context.json
│   └── sample-injection.md
│
└── README.md
```

## 22. 推荐第一版技术栈

这部分不是强制，只是建议。

### 前端管理界面

- Vue / Nuxt 或 React / Next.js；
- Tailwind CSS；
- IndexedDB 本地存储；
- 可后续接入后端 API。

### 浏览器插件

- Chrome Extension Manifest V3；
- TypeScript；
- Content Script 适配 ChatGPT / Claude / Gemini；
- Background Service Worker；
- IndexedDB 或 chrome.storage 存储轻量数据。

### 后端服务，可选

- Django / FastAPI；
- PostgreSQL；
- pgvector 或独立向量数据库；
- Redis 用于任务队列或缓存；
- Celery / RQ 处理异步摘要任务。

### 上下文处理

- LLM 摘要；
- 规则抽取；
- 嵌入向量；
- 关键词 + 语义混合检索；
- 敏感信息检测。

## 23. 第一版开发优先级

### P0：必须优先做

- Context Object 数据结构；
- 本地 Context Vault；
- 手动创建 / 编辑 Persona 和 Project；
- 生成注入 Prompt；
- 简单浏览器插件按钮；
- 在目标 AI 输入框中插入上下文。

### P1：第二步做

- 自动对话摘要；
- 自动项目识别；
- 自动偏好提取；
- 待确认记忆列表；
- 上下文来源追踪；
- 简单语义检索。

### P2：后续增强

- 自动注入策略；
- 跨设备同步；
- MCP Server；
- 团队空间；
- 企业权限；
- 审计日志；
- 数据脱敏；
- 多平台适配市场。

## 24. EchoMe 当前结论

EchoMe 当前最合理的切入点不是做一个大而全的 AI 聊天平台，也不是做传统导入导出迁移工具，而是先做一个跨 AI 的个人上下文 Companion。

第一阶段应重点跑通：

1. 从讨论中形成上下文；
2. 上下文可被用户编辑和确认；
3. 在另一个 AI 页面中可被一键注入；
4. 注入后 AI 的回答明显更贴合用户；
5. 逐渐从半自动走向自动、低打扰、无感接续。

如果这个闭环成立，EchoMe 后续可以自然扩展到 MCP、Agent、开发者工具、团队版和企业上下文治理。
