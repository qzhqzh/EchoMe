services:
  hub:
    build: ./hub
    ports:
      - "20000:20000"
    env_file:
      - ./hub/.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      embedding:
        condition: service_healthy
    environment:
      - ECHOME_EMBEDDING_URL=http://embedding:20002
    command: >
      sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 20000"

  web:
    build: ./web
    ports:
      - "20001:80"
    depends_on:
      - hub

  embedding:
    build: ./embedding
    ports:
      - "20002:20002"
    volumes:
      - embedding-models:/app/models
    environment:
      - MODEL_DIR=/app/models
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:20002/health')"]
      interval: 10s
      timeout: 30s
      retries: 3
      start_period: 120s

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: echome
      POSTGRES_PASSWORD: echome
      POSTGRES_DB: echome
    # No external port exposure - only accessible within docker network
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./hub/scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U echome"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    # No external port exposure - only accessible within docker network
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
  embedding-models:
